import { getPrefixedComponents } from '@porsche-design-system/components-react/prefixed-components';

export const PorscheDesignSystemMyPrefixComponents = getPrefixedComponents({ prefix: 'my-prefix' });

export const PorscheDesignSystemSomePrefixComponents = getPrefixedComponents({ prefix: 'some-prefix' });
